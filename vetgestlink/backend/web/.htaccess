# Servir arquivos da pasta uploads compartilhada
RewriteEngine On

# Se o arquivo existe na pasta uploads compartilhada, sirva-o
RewriteCond %{DOCUMENT_ROOT}/../uploads%{REQUEST_URI} -f
RewriteRule ^uploads/(.*)$ ../uploads/$1 [L]

